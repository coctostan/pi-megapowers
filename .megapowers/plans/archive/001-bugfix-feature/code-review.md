Clean — duplication removed, orphaned comment fixed, `hasOpenQuestions` now has clear separation.

## Updated Assessment
**ready** — Important issues (duplication + orphaned comment) fixed. The `session_shutdown` heuristic (Important #3) is a pre-existing debugging artifact, not part of this feature's scope — noted for a follow-up issue.